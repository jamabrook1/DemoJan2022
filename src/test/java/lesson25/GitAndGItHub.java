package lesson25;

public class GitAndGItHub {  // ~/eclipse-workspace/MavenProject (master) gitcommand for location


	// Git is Version Control tool, Git is primarily command line tool, it is open source repository
	// also there is Get UI WebBased app:
	
	// on our commandPrompt we put; git config --global user.name "firstname lastname" (for our name and lastname) 
								//git config --global user.email example@email.com  (for our email to be saved) 


	// git init - intialazes empty git repository
	// git add filename (add whatever file you choose)
	// git add * (adds all the file)
	// git commit -m( creates new version of file with an id) 
	// so we add file and commit it; git add filename ---- git commit -m "message"
	// git push (to send to a cloud/repository)
	// git pull ( to pull from the cloud/repository)
	// git status (shows a status of my commits) 
	//git clone <repository>
	//git remote add <repoName> <branchname>
	//git push <repoName> <branchname>
	//it pull <repoName> <branchname>	
	//git checkout -b <branchname>  - to create branch
	
	
	
	
}
