package lesson23;

public class PropertiesDemo {
	
	//key=value   // without a space 
	// in properties file we save values and by calling those names we can get value
	// in properties we keep application configuration data
	// Extension file name .properties
	// # hashtag sign we use for comments
	// = equal sign is distinguisher between key and value
	// we use FileOutputStream object to write into the file also we (setProperty) key word in order to write
	// we use FileInputStream object to read from the file, aslo we (getProperty) key word in order to get
	// FileOutputStream and FileInputStream object creates connection to that file
}
